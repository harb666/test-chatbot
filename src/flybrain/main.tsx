import React from "react";
import ReactDOM from "react-dom/client";
import FlyBrainApp from "./FlyBrainApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <FlyBrainApp />
  </React.StrictMode>,
);
